<UserControl x:Class="CoAPExplorer.WPF.Controls.Toolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoAPExplorer.WPF.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="130.42" d:DesignWidth="800">
    <materialDesign:ColorZone Mode="PrimaryMid" materialDesign:ShadowAssist.ShadowDepth="Depth1" materialDesign:ShadowAssist.ShadowEdges="Bottom"
                              HorizontalAlignment="Stretch" VerticalAlignment="Top" Padding="8" Height="56">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
            <TextBlock Text="{Binding Title}" VerticalAlignment="Bottom" Margin="8 0 8 8"
                       Style="{DynamicResource MaterialDesignTitleTextBlock}"/>
        </StackPanel>
    </materialDesign:ColorZone>
</UserControl>
