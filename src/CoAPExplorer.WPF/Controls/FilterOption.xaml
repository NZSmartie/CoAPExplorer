<UserControl x:Class="CoAPExplorer.WPF.Controls.FilterOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoAPExplorer.WPF.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" VerticalAlignment="Center" 
                Background="{StaticResource MaterialDesignPaper}">
        <ComboBox  materialDesign:HintAssist.Hint="(Select)"  Margin="0 0 8 0">
            <ComboBoxItem>Resource Type (rt=)</ComboBoxItem>
            <ComboBoxItem>Interface Type (if=)</ComboBoxItem>
            <ComboBoxItem>...</ComboBoxItem>
        </ComboBox>
        <TextBox  Margin="8 0 8 0" MinWidth="50" />
        <Button Style="{StaticResource MaterialDesignFlatButton}"
                ToolTip="MaterialDesignFlatButton">
            <materialDesign:PackIcon Kind="Plus"/>
        </Button>
    </StackPanel>
</UserControl>
